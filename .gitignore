cat > .gitignore << 'EOF'
bin/
obj/
*.user
*.suo
.vscode/
.idea/
*.DotSettings
.DS_Store
EOF

git rm -r --cached bin obj 2>/dev/null || true
git add .gitignore
git commit -m "Add .gitignore; stop tracking build artifacts"